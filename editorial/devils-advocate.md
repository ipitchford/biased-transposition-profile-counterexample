# Devil's Advocate review

Frozen artifact: original ZIP SHA-256
`03d8eae7e9e9cd263f5ba46d5443a0365eb13c59d917539a6a62a3361cbb50d2`.
The reviewer did not inspect other role reports.

**Recommendation:** Minor Revision. **Confidence:** 0.92 that the central
counterexample is sound; 0.70 on literature completeness and positioning.

The strongest attempted objection was that untouchedness is path-dependent
whereas the conjecture concerns final permutations. It fails because the event
actually compared is the final-state event `{F_B >= 2}` for a deterministic
slow set. Untouchedness is used only through the pathwise inclusion
`F_B >= A_B`. The one-step avoidance formula includes self-draws, its fixed
factorial moments converge to those of `Poisson(3)`, and the stationary subset
count converges to `Poisson(1/2)`. The resulting lower bound exceeds the
conjectured total-variation value at the pinned window point.

The role checked archive integrity, exact and optimized execution, the
avoidance identity, moment asymptotics, event comparison, Poisson threshold,
rational witness and the readable PDF. Required repairs were: inspect the 2025
conference source; state `0 < epsilon < 1` for Proposition 3; resolve identity
and rights; tighten replay/manifest language; and foreground the exact pinned
conjecture in the title. Optional improvements included floor robustness,
explicit event comparison, build versions and a standard Poisson justification.

The general `t_mix = t_star + O(n)` upper bound, a replacement profile, formal
verification, exhaustive priority, and replay of larger inherited runs remain
outside this verdict.
